const initState = {
  item: {},
  items: []
};

const listingsReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default listingsReducer;
